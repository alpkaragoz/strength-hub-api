package com.strengthhub.strength_hub_api.dto.request;

import lombok.Data;
import lombok.NoArgsConstructor;
import lombok.Builder;

@Data
@NoArgsConstructor
@Builder
public class CoachCodeGenerationRequest {
    // Empty for now, but can add future fields like description, custom expiry, etc.
}